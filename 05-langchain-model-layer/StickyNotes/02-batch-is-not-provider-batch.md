# Batch ≠ guaranteed provider batch API

The default batch contract can run ordinary invocations concurrently. Do not assume lower provider pricing or unlimited parallelism. Measure quota, latency, and failures.
