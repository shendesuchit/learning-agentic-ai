# Tools: a four-arrow loop

```text
Model asks → app checks → tool runs → ToolMessage returns
```

`bind_tools()` publishes a schema. It does not grant authorization, execute Python, validate arguments, or confirm a destructive action.
