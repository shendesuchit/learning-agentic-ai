# Typed output

```text
prompt → schema-guided response → Pydantic object → application checks
```

Pydantic proves shape/type. It does not prove the facts, the business rule, or the user’s permission.
