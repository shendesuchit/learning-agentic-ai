# Interface + configuration

```text
LangChain = common calling shape
Provider = real credentials + limits + capabilities

temperature  → variation
max_tokens   → answer budget
timeout      → wait budget
retries      → transient-failure policy
```

Remember: switching a model ID can preserve the method call while changing quality, latency, price, and features.
