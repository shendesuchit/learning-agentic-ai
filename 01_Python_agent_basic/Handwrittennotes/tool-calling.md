# Notebook Page: Tool Calling

> A Python function and a tool schema are related—but not identical.
>
> `function` → executable behaviour
>
> `schema` → model-readable description
>
> `registry` → approved name-to-function mapping
>
> Tool-call arguments arrive as structured JSON.
>
> Never confuse **requesting a function** with **executing a function**.

